mod git;
mod shadow;
mod channel;
mod err;

fn main() {
    // println!("{:?}",shadow::SystemEnv::new());

    //     use std::env;
    // for (key, value) in env::vars() {
    //     println!("{}: {}", key, value);
    // }
    //     println!("{:?}", env::var("RUSTC_WRAPPER"));
}